spring:
  application:
    name: fsr-ui
  thymeleaf:
    cache: false
    mode: HTML

fsr:
  serial:
    port: "/dev/ttyACM0"  # Cambiar según tu sistema (COM3 en Windows)
    baud-rate: 115200
    timeout: 1000
    num-sensors: 4
    enabled: false  # Cambiar a true cuando conectes un dispositivo
